#ifndef UTIL_TEXT_H_INCLUDED
#define UTIL_TEXT_H_INCLUDED

#include <string.h>
#include <stdio.h>
#include <ctype.h>

int word_inside(char *, const char *);
int scan(char *, int);
int is_number(char *num);
#endif
